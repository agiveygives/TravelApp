import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 16,
  },
  icon: {
    width: 40,
    height: 40,
  },
  text: {
    fontSize: 12,
    fontWeight: '400',
    marginHorizontal: 8,
  },
});

export default styles;
